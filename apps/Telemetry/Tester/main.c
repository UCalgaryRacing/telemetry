#include "can.h"
#include <stdio.h>
//#include <thread>

int main() {
  // Call a function to run the can read thread in C++

  // std::thread runReadCAN(readCAN);
  //runReadCAN();
  //runReadCAN.join();

  // Have a while loop that reads the global data
  // Print out the data
  return 0;
}