#include "can.h"

void initializeCAN() {
  // Start the .sh file, open the port
}

void readCAN() {
  // Read the data on the highest frequency of the sensors
  // Use mutex to write the data to the shared struct
}