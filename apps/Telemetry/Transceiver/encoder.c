#include "encoder.h"

EncodedData encode_data(unsigned int size, unsigned int timestamp)
{
    //sort the vairants from largest to smallest with respect to bytes

    //determine amount of memory we need to allocate 

    //push the number of sensor values we expect

    //insert timestamp

    //append the sensor ids after the timestamp

    //append the sensor values after the list of identifiers, in order

}